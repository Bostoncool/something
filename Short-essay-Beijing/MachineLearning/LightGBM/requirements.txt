pandas>=1.3.0
numpy>=1.17.0
matplotlib>=3.3.0
xarray>=0.18.0
netCDF4>=1.5.0
scikit-learn>=0.24.0
lightgbm>=4.0.0
# 注意：要使用GPU加速，需要安装支持GPU的LightGBM版本
# 方法1（推荐，使用conda）: conda install -c conda-forge lightgbm
# 方法2（使用pip + 环境变量）: LIGHTGBM_GPU=1 pip install lightgbm
# 方法3（从源码编译）: 
#   git clone --recursive https://github.com/microsoft/LightGBM
#   cd LightGBM
#   mkdir build && cd build
#   cmake -DUSE_GPU=1 ..
#   make -j4
#   cd ../python-package && python setup.py install
tqdm>=4.60.0
bayesian-optimization>=1.2.0

