import torch
print(f"PyTorch Version: {torch.__version__}")
print(f"CUDA is available: {torch.cuda.is_available()}")
print(f"CUDA Version: {torch.version.cuda}")

# Three different Python versions require different scheduling methods, but it's completed.
# Initially, it was necessary to manually add the path, then perform the installation.
